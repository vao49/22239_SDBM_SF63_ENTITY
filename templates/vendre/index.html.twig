{% extends 'base.html.twig' %}

{% block title %}Vendre index{% endblock %}

{% block body %}
    <h1>Vendre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Quantite</th>
                <th>PrixVente</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vendre in vendres %}
            <tr>
                <td>{{ vendre.quantite }}</td>
                <td>{{ vendre.prixVente }}</td>
                <td>
                    <a href="{{ path('app_vendre_show', {'idTicket': vendre.idTicket}) }}">show</a>
                    <a href="{{ path('app_vendre_edit', {'idTicket': vendre.idTicket}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_vendre_new') }}">Create new</a>
{% endblock %}
