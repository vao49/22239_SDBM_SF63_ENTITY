{% extends 'base.html.twig' %}

{% block title %}Vendre{% endblock %}

{% block body %}
    <h1>Vendre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantite</th>
                <td>{{ vendre.quantite }}</td>
            </tr>
            <tr>
                <th>PrixVente</th>
                <td>{{ vendre.prixVente }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vendre_index') }}">back to list</a>

    <a href="{{ path('app_vendre_edit', {'idTicket': vendre.idTicket}) }}">edit</a>

    {{ include('vendre/_delete_form.html.twig') }}
{% endblock %}
